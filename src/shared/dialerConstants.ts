/**
 * Shared design canvas constants for the dialer.
 * Used by main process (window sizing) and renderer (ScaleContainer).
 */
export const DESIGN_WIDTH = 320;
export const DESIGN_HEIGHT = 693;
export const COMPACT_DESIGN_HEIGHT = 380;
export const MAX_SCALE = 3;
